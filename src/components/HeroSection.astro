--- ---

<section class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-indigo-100 px-4 py-20">
  <div class="max-w-4xl mx-auto text-center">
    <h1 class="text-4xl md:text-6xl font-bold text-gray-900 mb-6 animate-fade-in">
      Vous recherchez un développeur ?
    </h1>
    <p class="text-lg md:text-xl text-gray-700 mb-8 max-w-2xl mx-auto animate-slide-up">
      Ça tombe bien ! Je suis passionné par le web et j'ai pu acquérir un certain nombre de compétences depuis mes débuts dans le web il y a déjà plus de 10 ans.
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center animate-bounce-in">
      <a
        href="#contact"
        class="btn-primary px-8 py-3 text-lg font-semibold rounded-lg shadow-lg hover:shadow-xl transition-all duration-300"
      >
        Me contacter
      </a>
      <a
        href="/CV-Allin.pdf"
        download
        class="bg-white text-gray-900 px-8 py-3 text-lg font-semibold rounded-lg shadow-lg hover:shadow-xl border border-gray-300 transition-all duration-300"
      >
        Télécharger CV
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes slide-up {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes bounce-in {
    from { opacity: 0; transform: scale(0.9); }
    to { opacity: 1; transform: scale(1); }
  }
  .animate-fade-in { animation: fade-in 1s ease-out; }
  .animate-slide-up { animation: slide-up 1s ease-out 0.2s both; }
  .animate-bounce-in { animation: bounce-in 1s ease-out 0.4s both; }
</style>