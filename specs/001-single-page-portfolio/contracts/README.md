# API Contracts

This feature is a static site with no server-side APIs. All interactions are client-side.

- Contact form: Uses mailto: or client-side handling for sending messages.
- No external API integrations required.